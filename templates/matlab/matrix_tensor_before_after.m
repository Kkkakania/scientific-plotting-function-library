function fig = matrix_tensor_before_after()
    addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','_utils','matlab'));
    fig = generated_template_figure('slope', 4420, 'matrix and tensor visualization: before-after slope', 'matrix and tensor visualization', 'before-after slope');
end
