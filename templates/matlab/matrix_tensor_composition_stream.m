function fig = matrix_tensor_composition_stream()
    addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','_utils','matlab'));
    fig = generated_template_figure('stacked_area', 4416, 'matrix and tensor visualization: composition stream', 'matrix and tensor visualization', 'composition stream');
end
