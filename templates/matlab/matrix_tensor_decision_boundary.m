function fig = matrix_tensor_decision_boundary()
    addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','_utils','matlab'));
    fig = generated_template_figure('decision_map', 4421, 'matrix and tensor visualization: decision boundary', 'matrix and tensor visualization', 'decision boundary');
end
