function fig = observer_estimation_composition_stream()
    addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','_utils','matlab'));
    fig = generated_template_figure('stacked_area', 1716, 'observer and state estimation: composition stream', 'observer and state estimation', 'composition stream');
end
