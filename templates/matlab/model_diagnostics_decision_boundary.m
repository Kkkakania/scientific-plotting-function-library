function fig = model_diagnostics_decision_boundary()
    addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','_utils','matlab'));
    fig = generated_template_figure('decision_map', 1521, 'model diagnostics: decision boundary', 'model diagnostics', 'decision boundary');
end
