function fig = paper_multipanel_composition_stream()
    addpath(fullfile(fileparts(mfilename('fullpath')),'..','..','_utils','matlab'));
    fig = generated_template_figure('stacked_area', 2216, 'paper multipanel layout: composition stream', 'paper multipanel layout', 'composition stream');
end
